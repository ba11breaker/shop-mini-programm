// pages/tools/edit-address.js
Page({

  /**
   * 页面的初始数据
   */
  data: {
    address: {

    }
  },

  /**
   * 生命周期函数--监听页面加载
   */
  onLoad: function (options) {
    const { type } = options;
    console.log(type);
    if(type == "edit"){

    }else if(type == "add"){
      
    }
  },
})